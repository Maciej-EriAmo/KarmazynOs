[build]
# Domyślnie nie ustawiamy target — `cargo test` host nie dotyczy no_std bin.
# Build freestanding:
#   cargo build --release --target x86_64-unknown-none

[target.x86_64-unknown-none]
rustflags = [
  "-C", "link-arg=-Tlinker.ld",
  "-C", "relocation-model=static",
]
